
<%= simple_form_for(@department) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :name ,:input_html => {:maxlength => 52, :size => 40} %>
    <%= f.input :description %>



      <%= f.label :person_id, "Owner" %>

    <%= f.association :person, collection: Person.order(:fullname),label_method: :fullname, prompt: "Choose an owner", label:"Owner" %>


    <br>
    <br>
    <%= f.input :active %>
    <%= f.input :user_id, :as =>  :hidden,input_html: {value:current_user.id} %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
<br>
